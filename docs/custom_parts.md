

## Adding your own modules & functions
*Nucli-Train* uses a registry framework to allow use of custom classes, functions, ... specific to your project. These custom objects can now be used internally by the *nucli-train* builders.

 following object registers

- `LOSS_REGISTRY`
- `PROCESSOR_REGISTRY`